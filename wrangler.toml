name = "hentai-api"
main = "src/worker.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[build]
command = ""

# Exclude Node.js-only packages that won't work on Workers
[build.upload]
format = "modules"

[vars]
NODE_ENV = "production"
